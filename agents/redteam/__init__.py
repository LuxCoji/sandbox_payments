"""LLM-driven red-team agent harness.

Builds on `sim.gateway` (capability-gated tool access) and `sim.chrono`
(branch forking for exploratory "try, observe, rollback" sessions). See
`docs/redteam_agent_design.md` for the architecture this implements.
"""
